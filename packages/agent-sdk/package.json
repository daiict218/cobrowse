{
  "name": "@cobrowse/agent-sdk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CoBrowse Agent SDK — thin library for vendor integration",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
