# Dependencies
node_modules/

# Environment (contains secrets)
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# IDE / tooling
.vscode/
.idea/
*.swp
*.swo
.claude/

# Coverage output
coverage/

# Build artifacts (SDK is rebuilt on deploy)
# Keep cobrowse.js + cobrowse.min.js committed so local dev works without build step
# packages/server/public/sdk/cobrowse.js
# packages/server/public/sdk/cobrowse.min.js
packages/server/public/sdk/*.map

# Tenant UI build output — committed so Railway deploy always has static files.
# Rebuild locally: npm run build:tenant-ui
# packages/server/public/tenant-ui/
