# Dependencies
node_modules/

# Environment (contains secrets)
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# IDE / tooling
.vscode/
.idea/
*.swp
*.swo
.claude/

# Coverage output
coverage/

# Build artifacts (SDK is rebuilt on deploy)
# Keep cobrowse.js + cobrowse.min.js committed so local dev works without build step
# packages/server/public/sdk/cobrowse.js
# packages/server/public/sdk/cobrowse.min.js
packages/server/public/sdk/*.map

# Tenant UI build output — rebuilt on Railway deploy via railway.toml buildCommand.
# For local dev: use `npm run dev:tenant-ui` (Vite dev server on port 5173).
# For production preview locally: run `npm run build:tenant-ui` first.
packages/server/public/tenant-ui/
